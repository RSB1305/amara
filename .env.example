# Public site origin used during the static production build.
PUBLIC_SITE_URL=https://amara-lodging.es

# Server-only secret. Never rename this with a PUBLIC_ prefix and never commit a value.
LODGIFY_API_KEY=
