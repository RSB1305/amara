---
import FrigilianaParkingPage from '../../page-families/location-authority/FrigilianaParkingPage.astro';

export function getStaticPaths() {
  return ['en', 'de', 'nl', 'sv'].map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
---

<FrigilianaParkingPage lang={lang} />
