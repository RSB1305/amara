---
import LegalNoticePage from '../../page-families/legal-notice/LegalNoticePage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
    { params: { lang: 'nl' } },
    { params: { lang: 'sv' } }
  ];
}

const { lang } = Astro.params;
---

<LegalNoticePage lang={lang} />