---
import DirectBookingBenefitsPage from '../../page-families/direct-booking-benefits/DirectBookingBenefitsPage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
    { params: { lang: 'nl' } },
    { params: { lang: 'sv' } }
  ];
}

const { lang } = Astro.params;
---

<DirectBookingBenefitsPage lang={lang} />
