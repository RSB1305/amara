---
import AmaraExperienceLandingPage from '../../page-families/amara-experience/AmaraExperienceLandingPage.astro';
import { getOwnedPageStaticPaths } from '../../lib/routeOwnership';
import type { AmaraLanguage } from '../../types/seo';

export function getStaticPaths() {
  return getOwnedPageStaticPaths();
}

const { lang } = Astro.params as { lang: AmaraLanguage };
---

<AmaraExperienceLandingPage lang={lang} />
