---
/**
 * AMARA root entry redirect
 *
 * Architectural intent:
 * The root path must not become a second homepage.
 * AMARA keeps language-prefixed routes as the canonical public structure.
 * This redirect provides a clean default entry point for Cloudflare Pages.
 */
return Astro.redirect('/en/');
---
