---
// Root should not become a second homepage.
// AMARA keeps language-prefixed routes as the canonical public structure.
// This redirect only provides a clean default entry point for Cloudflare Pages.
return Astro.redirect('/en/');
---
