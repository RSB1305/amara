---
import type { LinkToken } from '../lib/linkResolver';
// src/layouts/LocationAuthorityLayout.astro
import BaseLayout from './BaseLayout.astro';
import type { AmaraAuthoringSeo, AmaraLanguage } from '../types/seo';

interface Props {
  seo: AmaraAuthoringSeo;
  pageLang?: AmaraLanguage;
  footerHighlights?: { token: LinkToken; label: string }[];
  showContextNavigation?: boolean;
  bodyBackgroundClass?: string;
}

const {
  seo,
  pageLang,
  footerHighlights,
  showContextNavigation = false,
  bodyBackgroundClass
} = Astro.props;
---

<BaseLayout
  seo={seo}
  pageLang={pageLang}
  footerHighlights={footerHighlights}
  showContextNavigation={showContextNavigation}
  bodyBackgroundClass={bodyBackgroundClass}
>
  <!-- Global navigation stays before the layout-owned main landmark to preserve banner semantics across this page family. -->
  <slot name="navigation" />

  <main id="am-main-content" tabindex="-1" class="scroll-mt-[104px] lg:scroll-mt-[112px]">
    <slot />
  </main>
</BaseLayout>
