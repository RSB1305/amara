---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="am-support-float" href={href} target="_blank" rel="noopener" aria-label={label}>
  {label}
</a>
