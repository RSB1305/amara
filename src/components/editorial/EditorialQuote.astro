---
/**
 * EditorialQuote
 *
 * Centered pull-quote for trust/editorial pages. Visual parity with legacy
 * .am-direct-quote-block / .am-direct-quote (Direct Booking benefits).
 */
interface Props {
  quote: string;
  /** Optional attribution line (e.g. speaker). */
  author?: string;
  /** Optional source label; used when `author` is not set. */
  source?: string;
}

const { quote, author, source } = Astro.props;
const attribution = author ?? source;
---

<blockquote class="am-text-title mx-auto max-w-[1000px] text-center italic text-on-surface m-0">
    &ldquo;{quote}&rdquo;
    {
      attribution && (
        <footer class="mt-6 block am-text-body not-italic font-normal text-muted">
          &mdash; {attribution}
        </footer>
      )
    }
</blockquote>
