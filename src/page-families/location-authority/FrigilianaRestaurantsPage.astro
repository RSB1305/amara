---
import LocationExperienceHero from '../../components/location/LocationExperienceHero.astro';
import Navigation from '../../components/Navigation.astro';
import EditorialClosingCta from '../../components/editorial/EditorialClosingCta.astro';
import ExperienceContextNav from '../../components/experience/ExperienceContextNav.astro';
import ExperiencePreFooter from '../../components/experience/ExperiencePreFooter.astro';
import {
  frigilianaRestaurantsContent,
  frigilianaRestaurantsSeo
} from '../../content/frigilianaRestaurantsContent';
import LocationAuthorityLayout from '../../layouts/LocationAuthorityLayout.astro';
import { resolveOptionalLink, type LabeledLink } from '../../lib/linkResolver';
import { normalizeLanguage } from '../../lib/seo/resolve-seo-head';
import type { AmaraLanguage } from '../../types/seo';
import AuthorityFeatureSection from './AuthorityFeatureSection.astro';
import RestaurantSectionList from './RestaurantSectionList.astro';

interface Props {
  lang?: string;
}

const { lang = 'es' } = Astro.props;
const currentLang = normalizeLanguage(lang as AmaraLanguage);
const copy = frigilianaRestaurantsContent[currentLang];
const aboutHref = resolveOptionalLink('about', currentLang, {
  suppressMissing: true
});

if (!aboutHref) {
  throw new Error('[Frigiliana restaurants] Missing required link token: about');
}

const footerHighlights: LabeledLink[] = [
  {
    token: 'frigiliana_food_authority',
    label: copy.footerHighlights.restaurants
  },
  {
    token: 'frigiliana_beaches_authority',
    label: copy.footerHighlights.beaches
  },
  {
    token: 'frigiliana_hiking_authority',
    label: copy.footerHighlights.hiking
  }
];

const combineItems = copy.combine.items.map((item) => ({
  ...item,
  href: resolveOptionalLink(item.linkToken, currentLang, {
    suppressMissing: true
  })
}));

const heroImage = {
  src: '/images/hero-frigiliana.jpg',
  width: 1080
};
---

<LocationAuthorityLayout
  seo={frigilianaRestaurantsSeo}
  pageLang={currentLang}
  footerHighlights={footerHighlights}
>
  <Navigation
    slot="navigation"
    currentLang={currentLang}
    languageToken="frigiliana_food_authority"
  >
    <ExperienceContextNav slot="context-navigation" currentId="restaurants" lang={currentLang} />
  </Navigation>

  <div data-am-page="frigiliana-restaurants">
    <div class="w-full">
      <LocationExperienceHero
        topic="restaurants"
        eyebrow={copy.hero.kicker}
        title={copy.hero.title}
        paragraphs={copy.hero.paragraphs}
        imageSrc={heroImage.src}
        imageAlt={copy.hero.imageAlt}
        imageWidth={heroImage.width}
        byline={frigilianaRestaurantsSeo.article ? {
          lang: currentLang,
          authorName: frigilianaRestaurantsSeo.article.authorName,
          authorHref: aboutHref,
          updated: copy.overview.reviewed,
          note: copy.planning.note
        } : undefined}
      />

      <AuthorityFeatureSection
        id="choose"
        title={copy.overview.title}
        intro={copy.overview.intro}
        items={copy.overview.items}
      />

      <RestaurantSectionList
        sections={copy.restaurantSections}
        labels={copy.cardLabels}
      />

      <AuthorityFeatureSection
        id="planning"
        title={copy.planning.title}
        intro={copy.planning.intro}
        items={copy.planning.items}
      />

      <section class="am-section am-section--plain">
        <div class="max-w-3xl mb-16">
          <h2 class="am-text-title italic text-on-surface tracking-tight mb-6">
            {copy.combine.title}
          </h2>
          <p class="am-text-body text-on-surface-variant leading-relaxed">
            {copy.combine.intro}
          </p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-x-16 gap-y-14">
          {
            combineItems.map((item) => (
              <article class="border-t border-outline-variant/25 pt-8">
                <h3 class="am-text-subtitle text-on-surface mb-5">
                  {item.title}
                </h3>
                <p class="am-text-body text-on-surface-variant leading-relaxed">
                  {item.text}
                </p>
                {item.href && (
                  <p class="mt-5 am-text-body">
                    <a href={item.href} class="am-inline-link">
                      {item.linkLabel}
                      <span aria-hidden="true"> →</span>
                    </a>
                  </p>
                )}
              </article>
            ))
          }
        </div>
      </section>

      <ExperiencePreFooter currentId="restaurants" lang={currentLang} />

      <section class="max-w-screen-2xl mx-auto px-6 pt-24 md:px-12">
        <EditorialClosingCta
          title={copy.closing.title}
          lead={copy.closing.lead}
          lang={currentLang}
          links={[
            {
              token: 'romantic_hideaways',
              label: copy.closing.apartmentsCta,
              style: 'primary'
            },
            {
              token: 'book',
              label: copy.closing.availabilityCta,
              style: 'secondary'
            }
          ]}
        />
      </section>
    </div>
  </div>
</LocationAuthorityLayout>
