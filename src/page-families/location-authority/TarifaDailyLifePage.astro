---
import Navigation from '../../components/Navigation.astro';
import TarifaContextNav from '../../components/location/TarifaContextNav.astro';
import {
  tarifaDailyLifeContent,
  tarifaDailyLifeSeo
} from '../../content/tarifaDailyLifeContent';
import LocationAuthorityLayout from '../../layouts/LocationAuthorityLayout.astro';
import { createRequiredLinkResolver, type LabeledLink } from '../../lib/linkResolver';
import { normalizeLanguage } from '../../lib/seo/resolve-seo-head';
import type { AmaraLanguage } from '../../types/seo';
import AuthorityArticle from './AuthorityArticle.astro';
import AuthorityRelatedLinks from './AuthorityRelatedLinks.astro';
import AuthorityTextSection from './AuthorityTextSection.astro';

interface Props {
  lang?: string;
}

const { lang = 'es' } = Astro.props;
const currentLang = normalizeLanguage(lang as AmaraLanguage);
const content = tarifaDailyLifeContent[currentLang];

const requiredLink = createRequiredLinkResolver(currentLang, 'Tarifa daily life');

const aboutHref = requiredLink('about');
const locationHref = requiredLink('location_tarifa');
const propertyHref = requiredLink('casa');
const footerHighlights: LabeledLink[] = [
  { token: 'tarifa_daily_life', label: content.navLabel },
  { token: 'getting_to_tarifa', label: content.related.links[0].label },
  { token: 'tarifa_weather', label: content.related.links[3].label }
];
---

<LocationAuthorityLayout
  seo={tarifaDailyLifeSeo}
  pageLang={currentLang}
  showContextNavigation={false}
  footerHighlights={footerHighlights}
>
  <Navigation
    slot="navigation"
    currentLang={currentLang}
    languageToken="tarifa_daily_life"
    locationAuthorityNav={true}
  />
  <TarifaContextNav
    activeId="overview"
    activeChildId="daily-life-services"
    currentLabel="Tarifa"
    childLabel={content.navLabel}
    lang={currentLang}
  />

  <AuthorityArticle
    pageId="tarifa-daily-life"
    lang={currentLang}
    hero={content.hero}
    authorName={tarifaDailyLifeSeo.article!.authorName}
    authorHref={aboutHref}
    facts={content.facts}
    sources={content.sources}
    closing={content.closing}
    primaryCta={{ href: locationHref, label: content.closing.locationLabel }}
    secondaryCta={{ href: propertyHref, label: content.closing.propertyLabel }}
  >
    <div
      slot="hero-mark"
      aria-hidden="true"
      class="hidden font-serif text-[clamp(3.5rem,8vw,8.5rem)] leading-[0.72] text-primary/10 lg:col-span-4 lg:block"
    >
      5 min
    </div>

    {content.sections.map((section, index) => (
      <AuthorityTextSection
        section={section}
        index={index}
        data-am-daily-life-section={section.id}
      />
    ))}

    <AuthorityRelatedLinks
      related={content.related}
      columns="md:grid-cols-2"
      resolveHref={requiredLink}
    />
  </AuthorityArticle>
</LocationAuthorityLayout>
