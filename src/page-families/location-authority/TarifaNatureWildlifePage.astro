---
import { tarifaExperienceSpokeContent } from '../../content/tarifaExperienceSpokeContent';
import TarifaExperienceSpokePage from './TarifaExperienceSpokePage.astro';

interface Props { lang?: string; }
const { lang = 'es' } = Astro.props;
---

<TarifaExperienceSpokePage content={tarifaExperienceSpokeContent['nature-wildlife']} lang={lang} />
