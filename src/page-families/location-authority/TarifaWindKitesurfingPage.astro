---
import { tarifaGuideContent } from '../../content/tarifaGuideContent';
import TarifaGuidePage from './TarifaGuidePage.astro';

interface Props {
  lang?: string;
}

const { lang = 'es' } = Astro.props;
---

<TarifaGuidePage content={tarifaGuideContent.wind} lang={lang} />

