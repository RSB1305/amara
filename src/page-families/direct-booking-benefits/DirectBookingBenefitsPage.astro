---
import TrustPageLayout from '../../layouts/TrustPageLayout.astro';
import EditorialDefinitionList from '../../components/editorial/EditorialDefinitionList.astro';
import EditorialSplit from '../../components/editorial/EditorialSplit.astro';
import EditorialClosingCta from '../../components/editorial/EditorialClosingCta.astro';
import AmaraSection from '../../components/layout/AmaraSection.astro';
import type { EditorialClosingCtaLink } from '../../components/editorial/EditorialClosingCta.astro';
import InlineEmphasis from '../../components/text/InlineEmphasis.astro';
import AmaraActionLink from '../../components/actions/AmaraActionLink.astro';
import {
  directBookingBenefitsCopy,
  directBookingBenefitsFooterLabels,
  directBookingBenefitsSeo
} from '../../content/directBookingBenefitsContent';
import { resolveOptionalLink, type LabeledLink } from '../../lib/linkResolver';
import { normalizeLanguage } from '../../lib/seo/resolve-seo-head';
import type { AmaraLanguage } from '../../types/seo';

const { lang = 'es' } = Astro.props;
const currentLang = normalizeLanguage(lang as AmaraLanguage);
const seo = directBookingBenefitsSeo;
const copy = directBookingBenefitsCopy;
const footerHighlights: LabeledLink[] = [
  { token: 'amenities', label: directBookingBenefitsFooterLabels.amenities[currentLang] },
  { token: 'faq_general', label: directBookingBenefitsFooterLabels.faq_general[currentLang] },
  { token: 'instagram', label: directBookingBenefitsFooterLabels.instagram[currentLang] }
];
const directBookingDefinitionItems = copy.benefits.map((benefit) => ({
  title: benefit.title[currentLang],
  description: benefit.copy[currentLang]
}));
const closingCtaLinks: EditorialClosingCtaLink[] = [
  { label: copy.sections.ctaAvailability[currentLang], token: 'book', style: 'primary' as const },
  { label: copy.sections.ctaExplore[currentLang], token: 'romantic_hideaways', style: 'secondary' as const }
];
const heroCtaLinks = ([
  { token: 'book', label: copy.sections.ctaAvailability[currentLang] },
  { token: 'reviews_hub', label: copy.hero.ctaReviews[currentLang] }
] satisfies LabeledLink[])
  .map((link) => {
    const href = resolveOptionalLink(link.token, currentLang, { suppressMissing: true });
    return href ? { href, label: link.label } : null;
  })
  .filter((link): link is { href: string; label: string } => Boolean(link));
const inlineLinks = ([
  { token: 'contact', label: copy.sections.inlineContact[currentLang] },
  { token: 'reviews_hub', label: copy.sections.inlineReviews[currentLang] }
] satisfies LabeledLink[])
  .map((link) => {
    const href = resolveOptionalLink(link.token, currentLang, { suppressMissing: true });
    return href ? { href, label: link.label } : null;
  })
  .filter((link): link is { href: string; label: string } => Boolean(link));
---

<TrustPageLayout
  pageFamily="direct-booking"
  seo={seo}
  currentLang={currentLang}
  languageToken="direct_booking_benefits"
  footerHighlights={footerHighlights}
  pageId="direct-booking-benefits"
  hero={{
    subtitle: copy.hero.subtitle[currentLang],
    title: copy.hero.title[currentLang],
    lead: copy.hero.lead[currentLang],
    supportText: copy.hero.supportText[currentLang],
    ctaLinks: heroCtaLinks,
    ctaVariant: 'conversion'
  }}
>
  <AmaraSection labelledby="direct-booking-benefits-title" dataComponent="direct-booking-benefits">
    <EditorialSplit
      title={copy.sections.benefitsTitle[currentLang]}
      titleId="direct-booking-benefits-title"
    >
      <div class="mb-10">
        <EditorialDefinitionList items={directBookingDefinitionItems} />
      </div>
      <p class="am-text-body text-muted italic mb-8">
        <InlineEmphasis text={copy.sections.tip[currentLang]} />
      </p>
      <div class="flex flex-col flex-wrap items-start gap-4 sm:flex-row sm:items-center sm:gap-6">
        {inlineLinks.map((link) => (
          <AmaraActionLink href={link.href} variant="editorial">
            {link.label}
          </AmaraActionLink>
        ))}
      </div>
    </EditorialSplit>
  </AmaraSection>

  <AmaraSection labelledby="direct-booking-promise-title" dataComponent="direct-booking-promise">
    <div class="grid gap-10 lg:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)] lg:gap-20">
      <h2 id="direct-booking-promise-title" class="am-text-title text-on-surface">
        {copy.sections.promiseTitle[currentLang]}
      </h2>
      <div class="flex max-w-[65ch] flex-col gap-5 border-t border-border-subtle pt-6">
        <p class="am-text-body text-subtle">
          <InlineEmphasis text={copy.sections.promiseP1[currentLang]} />
        </p>
        <p class="am-text-body text-muted italic mt-3">
          <InlineEmphasis text={copy.sections.promiseNote[currentLang]} />
        </p>
      </div>
    </div>
  </AmaraSection>

  <EditorialClosingCta
    title={copy.sections.ctaTitle[currentLang]}
    titleId="direct-booking-next-step-title"
    lead={copy.sections.ctaLead[currentLang]}
    links={closingCtaLinks}
    lang={currentLang}
    spacing="page"
    dataComponent="direct-booking-next-step"
    role="conversion"
    family="trust"
  />
</TrustPageLayout>
