import { defineKnowledgeOpenQuestions } from '../../../schema';

// Research target. Questions are normalized from provider conflicts and missing first-party evidence.
export const tarifaParkingOpenQuestions = defineKnowledgeOpenQuestions([] as const);
